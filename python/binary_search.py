def first_false(lo: int, hi: int, predicate) -> int:\n    """Return the first x in [lo, hi] for which predicate(x) is False."""\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if predicate(mid):\n            lo = mid + 1\n        else:\n            hi = mid\n    return lo\n\n\ndef first_true(lo: int, hi: int, predicate) -> int:\n    """Return the first x in [lo, hi] for which predicate(x) is True."""\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if predicate(mid):\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo\n