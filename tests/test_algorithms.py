from python.binary_search import first_false, first_true\nfrom python.merge_sort import merge_sort\nfrom python.rabin_karp import find\nfrom python.graph import bfs, dfs\n\n\ndef test_boundaries():\n    assert first_true(0, 10, lambda x: x >= 7) == 7\n    assert first_false(0, 10, lambda x: x < 7) == 7\n\ndef test_merge_sort():\n    assert merge_sort([4, 1, 3, 1]) == [1, 1, 3, 4]\n\ndef test_rabin_karp():\n    assert find("abracadabra", "cada") == 4\n    assert find("hello", "xyz") == -1\n\ndef test_graph_traversals():\n    g = {1: [2, 3], 2: [4], 3: [], 4: []}\n    assert bfs(g, 1) == [1, 2, 3, 4]\n    assert dfs(g, 1) == [1, 2, 4, 3]\n